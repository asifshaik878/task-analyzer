# DEPLOY.md

## Purpose
Instructions to run a local production-like demo of the Smart Task Analyzer using Waitress (WSGI server) and Whitenoise (static file serving).

---

## Prerequisites
- Windows machine
- Python 3.8+
- Project cloned to folder (this repo)
- Virtual environment created and activated
  ```powershell
  python -m venv venv
  .\venv\Scripts\Activate.ps1
